matrix:
  include:
    - language: php
      php: 7.1
      install:
        - composer install --prefer-dist
      script:
        - vendor/bin/phpunit

    - language: node_js
      node_js: 6.11
      install:
        - yarn install
      script:
        - yarn cypress run

